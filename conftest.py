"""Root-level pytest fixtures shared across the whole suite.

Suggested fixtures to implement here:
    - config        : loaded settings for the target environment (session scope)
    - api_client    : configured requests session / base HTTP client
    - auth_token    : authenticated token for protected endpoints
    - test_user     : disposable user created/torn down per test or module
"""

# import pytest
